class Solution {
 /*
    public int singleNumber(int[] nums) {

        Set<Integer> ls = new HashSet<>();

        for(int num : nums){
            if(ls.contains(num)){
              ls.remove(num);
            }else{
        		ls.add(num);
            }
        }
    	return ls.iterator().next();
    }
*/
    //bit manipulation
    public int singleNumber(int[] nums) {

    			
    }
}