/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode(int x) { val = x; }
 * }
 */
class Solution {
    public int kthSmallest(TreeNode root, int k) {
        
    }

    /*
    public int DFS(TreeNode root,int kth){
    	
    }
    */

    public int BFS(TreeNode root, int kth){


    	//iterate all the nodes and add to a list
    	for(){

    	}

    	//sort them

    	//the return the kth one


    	//could use pirority quene/since it will sort itself
    }
}